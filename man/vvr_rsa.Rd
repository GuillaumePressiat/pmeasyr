% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VVR_MCO.R
\name{vvr_rsa}
\alias{vvr_rsa}
\title{~ VVR - preparer les rsa pour la valorisation}
\usage{
vvr_rsa(...)
}
\arguments{
\item{p}{Un noyau de paramètres \code{\link{noyau_pmeasyr}}}

\item{con}{Une connexion vers une db contenant les données PMSI}

\item{annee}{Dans le cas d'une con db, préciser l'année en integer sur deux caractères}
}
\value{
Un tibble contenant les variables des rsa nécessaires pour calculer les recettes ghs et suppléments
}
\description{
Importer ou collecter les variables des rsa nécessaires à leur valorisation GHS + suppléments
}
\details{
Deux méthodes sont disponibles : une utilisant l'import avec un noyau pmeasyr (p), l'autre utilisant les rsa stockés
dans une base de données (con)
}
\examples{
\dontrun{
# avec un noyau pmeasyr (importer les donnÃ©es)
annee <- 18
p <- noyau_pmeasyr(
  finess   = '750712184',
  annee    = 2000 + annee,
  mois     = 4,
  path     = '~/Documents/data/mco',
  progress = FALSE,
  n_max    = Inf,
  lib      = FALSE,
  tolower_names = TRUE)

vrsa <- vvr_rsa(p)

# depuis une base de donnÃ©es (collecter les donnÃ©es)
dbdir <- "~/Documents/data/monetdb"
con <- src_monetdblite(dbdir)

vrsa <- vvr_rsa(con, annee)
}

}
\seealso{
\code{\link{epmsi_mco_sv}}, \code{\link{vvr_ano_mco}}, \code{\link{vvr_mco}}
}
\author{
G. Pressiat
}
